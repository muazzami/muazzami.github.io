---
const { name, icon } = Astro.props;
---
<div style="text-align: center; flex: 1;">
  <img src={icon} alt={name} style="width: 60px; height: 60px; margin-bottom: 0.5rem;" />
  <div>{name}</div>
</div>
